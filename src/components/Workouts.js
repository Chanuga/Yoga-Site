import React from 'react';

// import data
import { workouts } from '../data';

const Workouts = () => {
  // destructure workouts data
  const { title, icon, programs } = workouts;
  return <section>workouts</section>;
};

export default Workouts;
